/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

*.gem

# A library, not an application: the lockfile is resolved fresh so that the
# ranges in the gemspec and Gemfile are what actually get tested.
/Gemfile.lock
/gemfiles/*.lock
